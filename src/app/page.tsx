import EmailLayout from "@/components/EmailLayout";

export const metadata = {
  title: "Mobile Email Compose Dashboard",
  description: "Send emails",
};

export default function Page() {
  return <EmailLayout />;
}
